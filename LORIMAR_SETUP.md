# Lorimar Weddings MCP continuation

The existing starter server was deployed successfully and its hello tool tested. This ZIP contains the next version, prepared locally: authentication and read-only Tripleseat access. It has not been deployed and no Tripleseat account is connected yet.

## Next action for Jennifer

The `lorimar-weddings-oauth` namespace has been created. Its ID, `f2d516f780854267990150f7cc7d5833`, is now configured in Wrangler.

Confirmed September 23: Tripleseat GET /v1/sites returned 200 and Lorimar Vineyards & Winery has site ID 6368 (customer ID 5877). TRIPLESEAT_SITE_ID=6368 is saved in Cloudflare and now included in Wrangler. The three runtime secrets are present; their hidden values are not verified. Jennifer confirmed she canceled the accidental Client ID edit. The dedicated OAuth application exists with the callback below and seven scopes; exact enabled scopes still need inspection.

The pasted live server.ts remains the hello sample. Upload all changed files together before deployment. This version requests leads:read, contacts:read, events:read, and sites:read; confirm these exact names/permissions in Tripleseat during live authorization. The callback is https://lorimar-weddings-mcp.patrick-07a.workers.dev/oauth/callback. Email sending and tour scheduling still require subsequent implementation.

## Changed files to transfer to the existing repository

- pnpm-lock.yaml
- examples/mcp-worker/package.json
- examples/mcp-worker/wrangler.jsonc
- examples/mcp-worker/env.d.ts (generated by Wrangler)
- examples/mcp-worker/src/server.ts
- examples/mcp-worker/src/auth.ts (new)
- examples/mcp-worker/src/connection.ts (new)
- examples/mcp-worker/tests/fake-workers.ts (new)
- examples/mcp-worker/tests/security.test.ts (new)
- examples/mcp-worker/tests/vitest.config.ts (new)
- examples/mcp-worker/README.md
- LORIMAR_SETUP.md (this file)

No credentials are included. Dependencies are recorded in the lockfile; node_modules and build output are excluded from the ZIP.

## Validation and remaining checks

The new MCP example passed its Vite production build, TypeScript check, and security test suite. The repository-wide `pnpm run check` was attempted but stopped when the environment denied the tsx IPC socket (EPERM). This is not a full repository-check pass. The security tests use mocked Tripleseat responses and mocked Durable Object storage; actual Cloudflare deployment, Tripleseat authorization/renewal, site membership, read scopes, and ChatGPT integration still need live acceptance tests.

The original root build compiles many unrelated example projects. Keep the already-working build configuration for the first authenticated deployment; optimize it only after validation.
